import metier.ListeUtilisateursImpl;
import metier.Utilisateur;

import javax.swing.*;


public class Statistique extends JFrame {
    public Statistique(ListeUtilisateursImpl userList, Utilisateur user) {
        super("Statistique");
    }
}